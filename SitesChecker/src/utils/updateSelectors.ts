import puppeteer from 'puppeteer-core';

const updateSelectors = async (
  currentPageSelector: CheerioStatic,
  prevPageSelector: CheerioStatic,

  selectors: any,
) => {
  return selectors;
}

export default updateSelectors;
