import getEnvConfig from './getEnvConfig';
import type { EnvConfig } from './getEnvConfig';

const initService = () => {};

export default initService;

// connect to infra db, take service name and routes + configs return express app
